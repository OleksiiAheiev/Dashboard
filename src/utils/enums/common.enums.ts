export enum Locale {
  EN = "en",
  UK = "uk"
}

export enum LocalStorage {
  THEME = "theme",
  SESSION = "session"
}

export enum Field {
  EMAIL = "email",
  PASSWORD = "password",
  CONFIRM_PASSWORD = "confirmPassword",
}